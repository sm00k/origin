#include <iostream>
#include <string>

using namespace std;

int main ()
{
   string s, p;
   cout << "enter s: ";
   cin >> s;
   cout << "enter p: ";
   cin >> p;

   for (int i = 0; i < (int)s.size(); ++i)
   {
       if (s[i] > p[i])
       {

       }
       else if (s[i] < p[i])
       {

       }
       else
       {

       }
   }
}
